#! /bin/sh

echo 'ls';
ls # Lists files and directories inside folder

echo 'ls -a';
ls -a # Lists all files including hidden files

echo 'ls -la';
ls -la # Long format list

echo 'ls -lS';
ls -lS # Long format list sorted by size